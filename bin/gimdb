#!/usr/bin/env ruby

$GIMDB_PATH = "#{File.dirname(__FILE__)}/.."
$DOMAIN     = 'gimdb'
$LOCALEDIR  = "#{$GIMDB_PATH}/data/locale"

require "#{$GIMDB_PATH}/src/gimdb.rb"

GimdbGlade.new("#{$GIMDB_PATH}/data/gimdb.glade")
Gtk.main
